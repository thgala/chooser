const defaultState = [];

export default function (state = defaultState, action) {
  return state
  // switch (action.type) {
  //   case 'GET_SOCIAL':
  //     return action.res.data.objects
  //   default:
  //     return state
  // }
}